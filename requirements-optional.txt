# Optional quality-mode dependencies.
#
# The default pipeline does not require these packages. Install them for
# stronger object masks:
#
#   pip install -r requirements-optional.txt

rembg
onnxruntime
